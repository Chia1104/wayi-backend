import { TrottlerBehignProxyGuard } from './throttler-behign-proxy.guard';

describe('TrottlerBehignProxyGuard', () => {
  it('should be defined', () => {
    expect(new TrottlerBehignProxyGuard()).toBeDefined();
  });
});
